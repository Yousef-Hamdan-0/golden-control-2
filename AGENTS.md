# تعليمات وملاحظات دائمة للمشروع

- التزم بأضيق نطاق ممكن: لا تعدّل شاشة أو سلوكًا غير مطلوب، وراجع فرق Git قبل التسليم.
- لا تعرض UUID للمستخدم. استخدم الرقم المخصص للعرض، واجعل UUID داخليًا لطلبات API فقط.
- اربط كل حقل API بالمتغير المطابق له ولا ترسل حقولًا غير موثقة.
- في جداول أداء الفنيين، مدد «ساعات الصيانة» و«ساعات الإنجاز» تُعرض بالدقائق إذا كانت أقل من 60 دقيقة، وبصيغة ساعات ودقائق إذا بلغت 60 دقيقة أو أكثر.
- عند ظهور تحذيرات `npm audit` أو `allow-scripts`، لا تنفذ `npm audit fix` أو توافق على install scripts تلقائيًا. راجع التغييرات والحزم (`sharp` و`puppeteer` و`unrs-resolver`) واطلب موافقة صريحة قبل أي تعديل واسع للحزم أو lockfile.
