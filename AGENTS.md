# تعليمات وملاحظات دائمة للمشروع

- التزم بأضيق نطاق ممكن: لا تعدّل شاشة أو سلوكًا غير مطلوب، وراجع فرق Git قبل التسليم.
- لا تعرض UUID للمستخدم. استخدم الرقم المخصص للعرض، واجعل UUID داخليًا لطلبات API فقط.
- اربط كل حقل API بالمتغير المطابق له ولا ترسل حقولًا غير موثقة.
- في جداول أداء الفنيين، مدد «ساعات الصيانة» و«ساعات الإنجاز» تُعرض بالدقائق إذا كانت أقل من 60 دقيقة، وبصيغة ساعات ودقائق إذا بلغت 60 دقيقة أو أكثر.
- عند ظهور تحذيرات `npm audit` أو `allow-scripts`، لا تنفذ `npm audit fix` أو توافق على install scripts تلقائيًا. راجع التغييرات والحزم (`sharp` و`puppeteer` و`unrs-resolver`) واطلب موافقة صريحة قبل أي تعديل واسع للحزم أو lockfile.

## سجل الأخطاء والحلول

### اختلاف تفاصيل الفاتورة بين الصفحات

- **الأعراض:** تفاصيل الفاتورة المفتوحة من «الأداء ← تفاصيل الطلب» لم تعرض اسم القطعة، ولم يظهر فيها زر «إرجاع الفاتورة»، بينما ظهرت البيانات والإجراءات بصورة صحيحة من «إدارة الفواتير».
- **السبب:** الصفحات كانت تستخدم مكوّن العرض الداخلي `InvoiceDetailsModal` مباشرة. صفحة إدارة الفواتير كانت وحدها تجهّز الفاتورة قبل العرض: تجلب التفاصيل والدفعات، وتحل اسم القطعة من مخزون القطع بواسطة `sparePartId`، وتربط إجراء الإرجاع حسب صلاحية المدير. مسار تفاصيل الطلب تجاوز هذه التجهيزات واعتمد نسخة الفاتورة المضمّنة في الطلب، والتي قد لا تحتوي اسم القطعة، ولم يمرر إجراء الإرجاع.
- **الحل:** إنشاء `ManagedInvoiceDetailsModal` بوصفه المدخل الرئيسي الوحيد لتفاصيل الفاتورة. يتولى هذا المكوّن جلب تفاصيل الفاتورة والدفعات، وإثراء أسماء القطع من مخزون القطع، وإظهار تأكيد «إرجاع الفاتورة» للمشرف، ثم يمرر الفاتورة المجهزة إلى مكوّن العرض الداخلي.
- **التوحيد:** أزرار تفاصيل الفاتورة في «إدارة الفواتير» و«تفاصيل الطلب» و«تفاصيل أمر الصيانة» تستخدم `ManagedInvoiceDetailsModal`. لا تستورد الصفحات أو الأزرار `InvoiceDetailsModal` مباشرة؛ يبقى مكوّنًا داخليًا لا يستخدمه إلا المكوّن الرئيسي.
- **منع تكرار المشكلة:** عند إضافة أي زر جديد لعرض تفاصيل فاتورة، اربطه بـ`ManagedInvoiceDetailsModal` فقط. تحقق بواسطة البحث أن الاستدعاء المباشر الوحيد لـ`InvoiceDetailsModal` موجود داخل `ManagedInvoiceDetailsModal.tsx`.
